<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Супергеройское приземление</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: linear-gradient(135deg, #0a0c10 0%, #1a1f2c 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', sans-serif;
        }
        
        .hero-container {
            width: 100%;
            max-width: 1200px;
            height: 600px;
            position: relative;
            overflow: hidden;
            border-radius: 32px;
            box-shadow: 0 50px 100px -20px rgba(0,0,0,0.5);
        }
        
        /* КАНВАС ДЛЯ АНИМАЦИИ */
        .animation-canvas {
            position: relative;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 30% 50%, #2a3f5e, #0b0e14);
        }
        
        /* ЭФФЕКТ ПЫЛИ ПРИ ПРИЗЕМЛЕНИИ */
        .dust-effect {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 30%;
            background: radial-gradient(ellipse at 50% 100%, rgba(255,215,0,0.3) 0%, transparent 70%);
            transform: scaleY(0);
            transform-origin: bottom;
            animation: dustRise 1.5s ease-out forwards;
            pointer-events: none;
            z-index: 5;
        }
        
        @keyframes dustRise {
            0% { transform: scaleY(0); opacity: 0; }
            30% { transform: scaleY(1.2); opacity: 0.8; }
            70% { transform: scaleY(1); opacity: 0.5; }
            100% { transform: scaleY(0.8); opacity: 0; }
        }
        
        /* СИЛУЭТ СУПЕРГЕРОЯ (РИСОВАННЫЙ) */
        .hero-silhouette {
            position: absolute;
            bottom: 10%;
            left: 20%;
            width: 200px;
            height: 300px;
            transform: translateY(100px) scale(0.8);
            opacity: 0;
            animation: heroLanding 1.2s cubic-bezier(0.2, 0.9, 0.3, 1.2) forwards;
            z-index: 10;
            filter: drop-shadow(0 20px 30px rgba(0,0,0,0.5));
            pointer-events: none;
        }
        
        @keyframes heroLanding {
            0% { transform: translateY(200px) scale(0.5); opacity: 0; }
            40% { transform: translateY(-20px) scale(1.1); opacity: 1; }
            70% { transform: translateY(0) scale(1); opacity: 1; }
            100% { transform: translateY(0) scale(1); opacity: 1; }
        }
        
        /* ПЛАЩ (ЭФФЕКТ РАЗВЕВАНИЯ) */
        .cape {
            position: absolute;
            top: 0;
            right: -30px;
            width: 100px;
            height: 250px;
            background: linear-gradient(135deg, #ffd700, #ffaa00);
            clip-path: polygon(20% 0, 100% 20%, 90% 80%, 10% 100%);
            opacity: 0.8;
            transform-origin: top left;
            animation: capeFlow 2s ease-in-out infinite;
            filter: blur(2px);
        }
        
        @keyframes capeFlow {
            0% { transform: rotate(5deg) skewX(-5deg); }
            50% { transform: rotate(15deg) skewX(5deg); }
            100% { transform: rotate(5deg) skewX(-5deg); }
        }
        
        /* ТЕЛО ГЕРОЯ */
        .hero-body {
            position: relative;
            width: 120px;
            height: 220px;
            background: #1a1f2c;
            border-radius: 60px 60px 30px 30px;
            box-shadow: inset -5px -10px 20px rgba(0,0,0,0.5), inset 5px 5px 15px rgba(255,215,0,0.2);
            border: 2px solid rgba(255,215,0,0.3);
        }
        
        /* ГОЛОВА */
        .hero-head {
            position: absolute;
            top: -40px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 80px;
            background: #2a2f3c;
            border-radius: 50%;
            border: 3px solid #ffd700;
            box-shadow: 0 5px 15px rgba(255,215,0,0.3);
        }
        
        /* МАСКА (ГЛАЗА) */
        .hero-mask {
            position: absolute;
            top: 25px;
            left: 15px;
            width: 50px;
            height: 25px;
            background: #ffd700;
            border-radius: 20px 20px 5px 5px;
            display: flex;
            justify-content: space-around;
            align-items: center;
        }
        
        .hero-mask::before,
        .hero-mask::after {
            content: '';
            width: 10px;
            height: 15px;
            background: #0a0c10;
            border-radius: 50%;
        }
        
        /* РУКА, УКАЗЫВАЮЩАЯ НА ТЕКСТ */
        .pointing-arm {
            position: absolute;
            top: 50px;
            right: -50px;
            width: 100px;
            height: 30px;
            background: #1a1f2c;
            border-radius: 30px;
            transform: rotate(-20deg);
            transform-origin: left center;
            animation: pointGesture 2s ease-in-out infinite;
            border: 2px solid #ffd700;
            box-shadow: 0 5px 15px rgba(255,215,0,0.3);
        }
        
        .pointing-arm::after {
            content: '';
            position: absolute;
            top: -15px;
            right: -10px;
            width: 30px;
            height: 30px;
            background: #1a1f2c;
            border-radius: 50%;
            border: 2px solid #ffd700;
        }
        
        @keyframes pointGesture {
            0% { transform: rotate(-20deg) translateX(0); }
            50% { transform: rotate(-15deg) translateX(10px); }
            100% { transform: rotate(-20deg) translateX(0); }
        }
        
        /* ТЕКСТ, НА КОТОРЫЙ УКАЗЫВАЕТ ГЕРОЙ */
        .text-container {
            position: absolute;
            top: 30%;
            right: 15%;
            width: 400px;
            z-index: 20;
            pointer-events: none;
        }
        
        .hero-text {
            opacity: 0;
            transform: translateX(50px);
            animation: textAppear 0.5s ease-out forwards;
            color: white;
            font-size: 2rem;
            font-weight: 700;
            text-shadow: 0 2px 10px rgba(255,215,0,0.3);
            margin-bottom: 15px;
            white-space: nowrap;
        }
        
        .hero-text:nth-child(1) { animation-delay: 0.8s; }
        .hero-text:nth-child(2) { animation-delay: 1.1s; }
        .hero-text:nth-child(3) { animation-delay: 1.4s; }
        
        @keyframes textAppear {
            0% { opacity: 0; transform: translateX(50px); }
            100% { opacity: 1; transform: translateX(0); }
        }
        
        /* ИСЧЕЗНОВЕНИЕ ГЕРОЯ */
        .hero-fadeout {
            position: absolute;
            bottom: 10%;
            left: 20%;
            width: 200px;
            height: 300px;
            pointer-events: none;
            animation: heroFade 0.8s ease-in forwards;
            animation-delay: 2.5s;
            z-index: 15;
        }
        
        @keyframes heroFade {
            0% { opacity: 1; transform: scale(1); }
            70% { opacity: 0.3; transform: scale(1.1); filter: blur(5px); }
            100% { opacity: 0; transform: scale(1.5); filter: blur(10px); }
        }
        
        /* СВЕЧЕНИЕ ПРИ ИСЧЕЗНОВЕНИИ */
        .fade-glow {
            position: absolute;
            bottom: 10%;
            left: 20%;
            width: 200px;
            height: 300px;
            background: radial-gradient(circle, rgba(255,215,0,0.3) 0%, transparent 70%);
            opacity: 0;
            animation: glowFade 1s ease-out forwards;
            animation-delay: 2.3s;
            pointer-events: none;
            z-index: 14;
        }
        
        @keyframes glowFade {
            0% { opacity: 0; transform: scale(0.8); }
            50% { opacity: 0.8; transform: scale(1.2); }
            100% { opacity: 0; transform: scale(1.8); }
        }
        
        /* МОЛНИИ (ЭФФЕКТ) */
        .lightning {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            background: linear-gradient(45deg, 
                transparent 30%, 
                rgba(255,215,0,0.1) 50%, 
                transparent 70%);
            animation: lightningFlash 2s ease-out;
            opacity: 0;
            z-index: 25;
        }
        
        @keyframes lightningFlash {
            0% { opacity: 0; }
            10% { opacity: 0.8; }
            20% { opacity: 0; }
            30% { opacity: 0.5; }
            40% { opacity: 0; }
            100% { opacity: 0; }
        }
        
        /* ОБЪЕДИНЯЕМ ВСЁ В ФИНАЛЬНУЮ СЦЕНУ */
        .hero-group {
            position: relative;
            width: 100%;
            height: 100%;
        }
        
        .hero-figure {
            animation: heroDisappear 0.8s ease-in forwards;
            animation-delay: 2.5s;
        }
        
        @keyframes heroDisappear {
            to { opacity: 0; }
        }
    </style>
</head>
<body>
    <div class="hero-container">
        <div class="animation-canvas">
            <!-- ЭФФЕКТ ПЫЛИ ПРИ ПРИЗЕМЛЕНИИ -->
            <div class="dust-effect"></div>
            
            <!-- ГРУППА ГЕРОЯ -->
            <div class="hero-group">
                <!-- СИЛУЭТ -->
                <div class="hero-silhouette hero-figure">
                    <div class="hero-body">
                        <div class="hero-head">
                            <div class="hero-mask"></div>
                        </div>
                        <div class="pointing-arm"></div>
                        <div class="cape"></div>
                    </div>
                </div>
                
                <!-- ЭФФЕКТ ИСЧЕЗНОВЕНИЯ -->
                <div class="fade-glow"></div>
                <div class="hero-fadeout"></div>
            </div>
            
            <!-- МОЛНИИ -->
            <div class="lightning"></div>
            
            <!-- ТЕКСТ, НА КОТОРЫЙ УКАЗЫВАЕТ ГЕРОЙ -->
            <div class="text-container">
                <div class="hero-text">⚡️ СУПЕРСИЛА</div>
                <div class="hero-text">⚡️ НЕУЯЗВИМОСТЬ</div>
                <div class="hero-text">⚡️ НЕВИДИМОСТЬ</div>
            </div>
        </div>
    </div>
</body>
</html>
